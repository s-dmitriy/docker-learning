sudo: required
services: 
    - docker

before_install:
    - docker build -t dmitryshay/docker-learning -f Docker.dev .

script:
    - docker run dmitryshay/docker-learning npm run test -- --coverage